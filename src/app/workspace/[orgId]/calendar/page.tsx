import { PageStub } from "@/components/workspace/page-stub";

export const metadata = { title: "Calendar · CarHire" };

export default function CalendarPage() {
  return (
    <PageStub
      title="Calendar"
      description="Bookings, checkouts and returns at a glance."
    />
  );
}
